import{saveMaintenanceAction}from"@/app/maintenance/actions";const labels={DAILY:"Hằng ngày",WEEKLY:"Hằng tuần",MONTHLY:"Hằng tháng"}as const;export function MaintenanceForm({occurrenceId,cadence,assetName}:{occurrenceId:string;cadence:keyof typeof labels;assetName:string}){return <form action={saveMaintenanceAction} className="space-y-5 rounded-3xl border bg-white p-5"><input type="hidden" name="occurrenceId" value={occurrenceId}/><input type="hidden" name="cadence" value={cadence}/><header><p className="text-sm font-bold text-blue-800">{labels[cadence]}</p><h2 className="text-xl font-bold">{assetName}</h2></header><label className="block font-semibold">Ngày giờ thực hiện<input required name="performedAt" type="datetime-local" className="mt-2 min-h-11 w-full rounded-xl border px-3"/></label><fieldset><legend className="font-bold">Kết quả</legend><div className="mt-2 grid grid-cols-2 gap-3"><label className="flex min-h-12 items-center gap-2 rounded-xl border p-3"><input required type="radio" name="result" value="PASS"/>Đạt</label><label className="flex min-h-12 items-center gap-2 rounded-xl border p-3"><input required type="radio" name="result" value="FAIL"/>Không đạt</label></div></fieldset><label className="block font-semibold">Ghi chú<textarea name="note" className="mt-2 min-h-24 w-full rounded-xl border p-3"/></label><button className="min-h-12 w-full rounded-xl bg-blue-800 font-bold text-white">Lưu bảo dưỡng</button></form>}
