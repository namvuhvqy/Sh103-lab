import{saveDecontaminationAction}from"@/app/areas/[areaCode]/knbm/actions";export function DecontaminationForm({periodId,areaCode,today}:{periodId:string;areaCode:string;today:string}){return <form action={saveDecontaminationAction} className="space-y-5 rounded-3xl border bg-white p-5"><input type="hidden" name="periodId" value={periodId}/><input type="hidden" name="areaCode" value={areaCode}/><label className="block font-semibold">Ngày thực hiện<input required type="date" name="businessDate" defaultValue={today} className="mt-2 min-h-11 w-full rounded-xl border px-3"/></label><fieldset><legend className="font-bold">Hoạt động đã thực hiện</legend><div className="mt-3 space-y-2">{[["daily","Hằng ngày"],["weekly","Hằng tuần"],["spill","Xử lý tràn đổ"]].map(([name,label])=><label key={name} className="flex min-h-12 items-center gap-3 rounded-xl border p-3"><input type="checkbox" name={name} className="h-5 w-5"/>{label}</label>)}</div></fieldset><label className="block font-semibold">Ghi chú<textarea name="note" className="mt-2 min-h-24 w-full rounded-xl border p-3"/></label><button className="min-h-12 w-full rounded-xl bg-blue-800 font-bold text-white">Lưu khử nhiễm</button></form>}
