import{notFound}from"next/navigation";import{AppShell}from"@/components/shell/AppShell";import{MaintenanceForm}from"@/components/forms/MaintenanceForm";import{getOccurrence}from"@/lib/forms/queries";export default async function Page({params,searchParams}:{params:Promise<{occurrenceId:string}>;searchParams:Promise<{error?:string}>}){const id=(await params).occurrenceId,query=await searchParams;const occurrence=await getOccurrence(id);if(!occurrence)notFound();const period=occurrence.register_periods;const cadence=occurrence.slot_code as "DAILY"|"WEEKLY"|"MONTHLY";return <AppShell headerTitle="Bảo dưỡng thiết bị"><div className="mx-auto max-w-xl"><p className="text-sm font-bold text-blue-800">BM.02/QL.TRTB.01</p><h1 className="mt-1 text-3xl font-bold">Ghi nhận bảo dưỡng</h1>{query.error?<p role="alert" className="mt-4 rounded-xl bg-red-50 p-3 font-semibold text-red-800">{query.error}</p>:null}<div className="mt-6"><MaintenanceForm occurrenceId={occurrence.id} cadence={cadence} assetName={period.assets?.source_name??"Thiết bị"}/></div></div></AppShell>}
