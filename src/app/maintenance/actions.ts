"use server";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{validateMaintenance}from"@/lib/forms/validation";export async function saveMaintenanceAction(f:FormData){const cadence=String(f.get('cadence')),result=String(f.get('result'));const v=validateMaintenance(cadence,result);if(!v.ok)throw new Error(v.error);const raw=String(f.get('performedAt'));if(!raw)throw new Error('Ngày giờ thực hiện là bắt buộc');const s=await createClient();const{error}=await s.rpc('save_maintenance_record',{target_occurrence_id:String(f.get('occurrenceId')),target_performed_at:new Date(raw).toISOString(),target_cadence:cadence,target_result:result,target_note:String(f.get('note')||'')||null});if(error)throw new Error(error.message);redirect('/tasks?saved=maintenance')}
