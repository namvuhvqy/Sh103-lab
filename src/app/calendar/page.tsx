import Link from'next/link';import{AppShell}from'@/components/shell/AppShell';import{TaskList}from'@/components/forms/TaskList';import{getTodayTasks}from'@/lib/forms/queries';export const dynamic='force-dynamic';export default async function Page({searchParams}:{searchParams:Promise<{date?:string;area?:string}>}){const q=await searchParams;const date=q.date??new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Ho_Chi_Minh'}).format(new Date());const tasks=await getTodayTasks(date);return <AppShell headerTitle="Lịch công việc"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold text-blue-800">Ngày / Tuần / Tháng</p><h1 className="text-3xl font-bold">Lịch công việc</h1></div><form className="flex gap-2"><input type="date" name="date" defaultValue={date} className="min-h-11 rounded-xl border px-3"/><button className="rounded-xl bg-slate-950 px-4 font-bold text-white">Xem ngày</button></form></div><div className="mt-6"><TaskList tasks={tasks} area={q.area}/></div><Link href="/periods" className="mt-6 inline-flex min-h-11 items-center rounded-xl border px-4 font-bold">Xem theo tháng / kỳ sổ</Link></AppShell>}
