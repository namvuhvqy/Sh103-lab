"use server";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{validateDecontamination}from"@/lib/forms/validation";export async function saveDecontaminationAction(f:FormData){const daily=f.get('daily')==='on',weekly=f.get('weekly')==='on',spill=f.get('spill')==='on';const v=validateDecontamination({daily,weekly,spill});if(!v.ok)throw new Error(v.error);const s=await createClient();const area=String(f.get('areaCode'));const{error}=await s.rpc('save_decontamination_record',{target_period_id:String(f.get('periodId')),target_business_date:String(f.get('businessDate')),target_daily:daily,target_weekly:weekly,target_spill:spill,target_note:String(f.get('note')||'')||null});if(error)throw new Error(error.message);redirect(`/areas/${area}/knbm?saved=1`)}
