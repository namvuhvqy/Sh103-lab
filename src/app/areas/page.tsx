import { AppShell } from "@/components/shell/AppShell"; import { AreaCard } from "@/components/areas/AreaCard"; import { getAreaSummaries } from "@/lib/forms/queries";
export const dynamic='force-dynamic'; export default async function AreasPage(){const areas=await getAreaSummaries();return <AppShell headerTitle="Khu vực làm việc"><header><p className="text-sm font-bold uppercase tracking-wider text-blue-800">Area-first</p><h1 className="mt-1 text-3xl font-bold text-slate-950">Chọn khu vực tác nghiệp</h1><p className="mt-2 text-slate-600">Thiết bị và phiếu được tổ chức theo nơi làm việc thực tế.</p></header><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{areas.map(a=><AreaCard key={a.code} {...a}/>)}</div></AppShell>}
