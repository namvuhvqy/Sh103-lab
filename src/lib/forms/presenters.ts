export type OccurrenceDisplayState='PENDING'|'COMPLETED'|'N_A'|'OVERDUE';export function occurrenceState(value:{status:string;windowEnd:string},now=new Date()):OccurrenceDisplayState{if(value.status==='COMPLETED')return'COMPLETED';if(value.status==='N_A')return'N_A';return new Date(value.windowEnd)<now?'OVERDUE':'PENDING'}export function entryHref(code:string,occurrenceId:string,areaCode?:string){if(code==='BM.01_KNBM'&&areaCode)return`/areas/${areaCode}/knbm`;if(code==='BM.02/QL.TRTB.01')return`/maintenance/${occurrenceId}`;if(code==='BM.06/QL.TRTB.01')return`/bm06/${occurrenceId}`;return`/entry/${occurrenceId}`}